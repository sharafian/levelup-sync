{
  "name": "levelup-sync",
  "version": "1.0.0",
  "description": "Provide synchronous interface for levelup (using a cache) to avoid race conditions.",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0"
}
